// Permission Types & Enums
export enum Resource {
  WORKSPACE = 'workspace',
  PROJECT = 'project',
  ISSUE = 'issue',
  CYCLE = 'cycle',
  ROADMAP = 'roadmap',
  USER = 'user',
  COMMENT = 'comment',
  INTEGRATION = 'integration',
  AUDIT_LOG = 'audit_log',
  ANALYTICS = 'analytics',
}
